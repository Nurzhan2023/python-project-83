[project]
name = "hexlet-code"
version = "0.1.0"
description = "Hexlet Flask App"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask",
    "gunicorn",
    "python-dotenv",
]

[tool.uv]
virtualenv = ".venv"
